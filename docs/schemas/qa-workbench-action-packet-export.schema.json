{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qa-pilot.carbideframe/schemas/qa-workbench-action-packet-export-v1",
  "title": "QA Workbench Action Packet Export",
  "description": "Schema for a bounded action packet export. Exports package the intended action path for downstream handoff only. The export does not execute work, authorize execution, approve intake, verify evidence, close items, mutate packets, mutate sources, or seal anything.",
  "type": "object",
  "required": [
    "export_id",
    "source_action_packet_id",
    "source_receipt_id",
    "source_summary_id",
    "source_intake_id",
    "source_item_ids",
    "source_evidence_ids",
    "action_state",
    "decision",
    "rationale",
    "exported_at",
    "authority_disclaimer",
    "advisory_only",
    "custody",
    "librarian_impact"
  ],
  "properties": {
    "export_id": {
      "type": "string",
      "pattern": "^AXPK-[A-Z0-9]+-[0-9]{4,6}$",
      "description": "Unique action export identifier (e.g. AXPK-ACTION-0001)."
    },
    "source_action_packet_id": {
      "type": "string",
      "pattern": "^AP-",
      "description": "The action packet ID that this export derives from."
    },
    "source_receipt_id": {
      "type": "string",
      "pattern": "^WDR-",
      "description": "The review decision receipt ID."
    },
    "source_summary_id": {
      "type": "string",
      "pattern": "^DS-",
      "description": "The decision summary ID."
    },
    "source_intake_id": {
      "type": "string",
      "pattern": "^IR-",
      "description": "The intake record ID."
    },
    "source_item_ids": {
      "type": "array",
      "items": { "type": "string", "pattern": "^QA-" },
      "minItems": 1,
      "description": "Workbench item IDs relevant to this export."
    },
    "source_evidence_ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Evidence artifact IDs relevant to this export."
    },
    "action_state": {
      "type": "string",
      "enum": ["proposed", "owner_authorized", "deferred", "rejected"],
      "description": "The action state from the source packet."
    },
    "decision": {
      "type": "string",
      "enum": ["accepted_for_action", "authorized", "deferred", "rejected"],
      "description": "The Owner decision from the source packet."
    },
    "rationale": {
      "type": "string",
      "minLength": 3,
      "maxLength": 2000,
      "description": "Owner rationale for the action."
    },
    "exported_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the export was created."
    },
    "authority_disclaimer": {
      "type": "string",
      "const": "This action packet export packages the intended action path for downstream handoff only. It does not execute work, authorize execution, approve intake, verify evidence, close workbench items, mutate packets, mutate source records, seal anything, or create autonomous authority. Custody is qa-pilot-local. Librarian impact is none.",
      "description": "Standard authority disclaimer. Must match exactly."
    },
    "custody": {
      "type": "string",
      "const": "qa-pilot-local",
      "description": "Must always be qa-pilot-local."
    },
    "advisory_only": {
      "type": "boolean",
      "const": true,
      "description": "Must always be true. Exports are advisory-only."
    },
    "librarian_impact": {
      "type": "string",
      "const": "none",
      "description": "Must always be none."
    }
  },
  "additionalProperties": false
}
