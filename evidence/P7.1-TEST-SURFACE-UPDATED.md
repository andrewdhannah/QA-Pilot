# P7.1 Test Surface — Updated After P7.1-OPS-003

**Date:** 2026-08-17
**Status:** ACTIVE
**Authorization:** Owner-directed (P7.1-OPS-003 disposition)

---

## Test Surface Definition

### Included (operational)

| Component | Source | Authority | Purpose |
|---|---|---|---|
| Startup contract | `active/qa-pilot/startup-contract.json` | contract | Defines project identity, required files, MCP context |
| Capability declaration | Adapter `capability_discovery` | adapter | Detects Librarian service availability |
| MCP availability | MCP health probe | runtime | Confirms MCP endpoint reachable |
| Project context | `project_assemble_context` | declared tool | 8-section agent context packet |
| Governance profile | `project_get_profile` | declared tool | Module configuration |
| Lifecycle cursor | `project_get_cursor` | declared tool | Phase position |
| Allowed transitions | `project_get_allowed_transitions` | declared tool | Valid state changes |
| Validation pipeline | QA Pilot validators | project | Schema validation, test composition, result export |
| Evidence stores | `data/evidence/`, `data/test-cases/`, `data/result-packets/` | project | Artifacts and indices |
| Flightplan | `flightplan_*` tools | separate system | Token tracking, session lifecycle |

### Excluded (not authorized for P7.1)

| Component | State | Reason |
|---|---|---|
| Session checkout/checkin | Not in project contract | Requires authorization bridge |
| Heartbeat lifecycle | Not in project contract | Requires authorization bridge |
| Session mutation APIs | Not in project contract | Requires authorization bridge |
| Work order dispatch | Requires decision ID | Authorization basis not established |
| Agent governance authorization | Requires entity/intent/actions | Authorization basis not established |

---

## Control Variable

**P7.1 tests whether the declared governance surface improves work quality and Owner decision visibility.**

The control is: work executed with declared governance tools vs. work executed without them.

The excluded tools (session lifecycle) are a separate capability layer that requires future authorization. P7.1 does not depend on them.

---

## Evidence Chain

1. **P7.1-GOV-WO-001:** First governed work item. Governance tools unavailable. Work executed with direct files + Flightplan. Evidence produced. → Positive governance outcome despite tool unavailability.

2. **P7.1-OPS-001:** Session tool availability finding. → Recorded as operational finding.

3. **P7.1-OPS-002:** FlightPlan session provenance. → Resolved (closed as stale).

4. **P7.1-OPS-003:** Session tool availability characterization. → RESOLVED BY EVIDENCE. Expected capability boundary. Accept limitation.

5. **P7.1-OPS-004:** [Next work item] → Will test declared governance surface.

---

## Next Work Item Criteria

The next P7.1 work item must:

1. Be a real operational task (not infrastructure investigation)
2. Use only the declared tool surface
3. Produce measurable evidence about governance value
4. Record the capability gap as context
5. Maintain the distinction: requested / authorized / executed / verified

---

## Capability Gap (observed, not defect)

```
Capability:    Session management authorization bridge
State:         Designed / available at MCP layer
Operational:   Not in project contract
Authorization: Required before activation
P7.1 impact:   Test surface restricted to declared tool subset
```

This gap should not silently become permanent. It remains an observed capability boundary requiring Owner authorization before activation.
